@model AgendaTelefonica.Models.CompaniaTelefonica

@{
    ViewData["Title"] = "Eliminar Empresa Telefónica";
}

<h1 class="text-light">Eliminar Empresa</h1>

<h3 class="text-light">Confirma la eliminación a continuación</h3>
<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2 text-light">
            @Html.DisplayNameFor(model => model.NombreCompania)
        </dt>
        <dd class = "col-sm-10 text-light">
            @Html.DisplayFor(model => model.NombreCompania)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdCompania" />
        <input type="submit" value="Eliminar" class="btn btn-danger" /> |
        <a asp-action="Index">Volver</a>
    </form>
</div>
